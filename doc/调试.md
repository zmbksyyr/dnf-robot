# 全流程调试与压测检查清单

本文是每次发布前的待执行清单。所有项目默认未勾选；完成一次调试后再按实际结果打勾和补充记录。

## 本次执行记录

时间：`2026-06-18 16:19` 开始。

- [x] 本地 `go test ./...` 通过。
- [x] 本地 `go vet ./...` 通过。
- [x] Linux 版 `robot` 构建通过。
- [x] VM `快照 2` 可恢复，恢复后可 SSH。
- [x] 已部署当前 `bin/robot-linux` 到 `/root/robot`。
- [x] 已清理快照中残留的 `/root/gateway` 进程，8111/8112 由 `/root/robot` 占用。
- [x] Web 登录成功，密码 `twadmin`。
- [x] `autoStatus`、`systemStatus`、`keypairStatus`、`robotConfigGet`、`robotsStatus count=10`、`robotsStatus count=10000` 均可返回。
- [x] Web `/api/game-port` 可读取游戏端口，观察到 `cain01 max_user_num=1000`。
- [x] Web `/api/log` 可读取 `/root/config/log_robot`。
- [x] 默认目标 `20` 能启动并收敛到 `actors=20 leased=20 idle=0 blocked=0`。
- [x] 20 人窗口观察约 `9分40秒`，actor 和租约稳定，未因 `running` 波动补 actor。
- [x] 20 人窗口内移动、普通喊话、全服喊话、摆摊成功、摆摊失败、摆摊过期均有增长。
- [x] 配置热更新生效：目标人数改为 `30`，摆摊概率改为 `0`，摆摊并发改为 `1`。
- [x] Web 假人列表返回全部假人，30 人时返回 `17000000..17000029`，扩容优先小 UID。
- [x] 创建角色装备探针已补测。按 `uid -> cid/charac_no -> inventory.equipslot` 解压装备 blob。
- [x] 手动移动、手动喊话、手动上线/下线、手动摆摊已补测。
- [x] 100 人 10 分钟已执行。
- [x] 600 人 10 分钟已执行，修复误回收后通过。
- [x] 600 恢复 20 缩容已执行，修复后约 10 秒收敛。
- [x] Key 缺失/恢复已执行。
- [x] 游戏端口关闭/恢复已执行。

阻塞：

- [ ] 第一轮在默认 20 人窗口约 `9分40秒` 后，HTTP 出现连接重置，随后 VM 的 `22/8111/8112/10011` 全部连接超时。
- [ ] 恢复 `快照 2`、重新部署 `/root/robot` 后，第二轮在热更新和手动操作阶段再次出现 VM 整体断网，SSH/Web 均超时。
- [ ] 当前已停止本地调试 runner；VM 启动/运行状态出现过 `vmrun list` 与 `vmware-vmx` 不一致，需要先恢复稳定 VM 环境后再继续 100/600 压测。

继续执行：`2026-06-18 17:20` 后，VM 网络等待恢复，重新部署当前 `/root/robot`，继续未完成项。

- [x] VM 高负载/启动阶段会导致 SSH/Web 短时超时，等待后恢复，不立即判定 VM 死机。
- [x] 恢复后再次确认 8111/8112 由 `/root/robot` 占用，游戏端口 `10011` 可用。
- [x] 手动移动指定 UID 成功。样本 UID `17000000`，`accepted=1 confirmed=1 failed=0`。
- [x] 手动普通喊话指定 UID 成功。样本 UID `17000000`，`accepted=1 confirmed=1 failed=0`。
- [x] 手动全服喊话指定 UID 成功。样本 UID `17000000`，`accepted=1 confirmed=1 failed=0`。
- [x] 手动下线指定 UID 成功。样本 UID `17000000`，`accepted=1 confirmed=1 failed=0`。
- [x] 手动上线指定 UID 成功。样本 UID `17000000`，`accepted=1 confirmed=1 failed=0`。
- [x] 手动摆摊异步命令可入队。样本 UID `17000000`，`robotsStoreAsync state=queued`。
- [x] 100 人 10 分钟完成，`actors=100 leased=100` 稳定。
- [x] 100 人窗口移动、普通喊话、全服喊话、摆摊均持续增长。窗口增量：`move +3733`、`local +1937`、`world +1947`、`store +124`。
- [x] 100 人窗口资源稳定。结束时约 `CPU 2.48% / 102MB / 427 goroutines`。
- [ ] 100 人窗口 Web 列表数量异常。目标/租约为 100，但 `robotsStatus count=10000` 结束时 `total=97 returned=97`。
- [ ] 600 人 10 分钟完成但未通过。10 分钟结束：`target=600 actors=600 leased=387 idle=213 running=49 connecting=212`。
- [ ] 600 扩容速度异常。目标改为 600 后，`actors` 约 140 秒才从 100 跳到 600；`leased` 长时间停在 `159/272/387`，10 分钟未达到 600。
- [x] 600 窗口移动、普通喊话、全服喊话、摆摊仍持续增长。窗口增量：`move +4822`、`local +2194`、`world +2208`、`store +49`。
- [x] 600 窗口未观察到资源失控。结束时约 `CPU 11.04% / 180MB / 1996 goroutines`，`blocked_uids=0`。
- [ ] 600 窗口 Web 列表数量异常。结束时 `total=358 returned=358`，UID 区间 `17000000..17000384`。
- [ ] `600 -> 20` 缩容未通过。`target_online=20` 后 3 分钟内仍为 `actors=600 leased=387 idle=213`。
- [ ] `autoStop` 停止不及时。`enabled=false` 后约 120 秒动作仍继续增长，约 140 秒后 `actors=0 leased=0`，约 190 秒后 `connecting=0`。
- [x] `autoStop` 最终能回收到 `actors=0 leased=0 running=0 connecting=0`。
- [x] 停止后重新启动默认 20 人成功。约 40 秒后 `actors=20 leased=20 running=17`。
- [ ] UID/假人数量膨胀。恢复到 20 人后，Web 列表仍有 `435` 个假人，UID 已到 `17000434`，虽然前 20 个仍为 `17000000..17000019`。
- [ ] 远程快照中的旧 `robot_config.ini` 仍含旧注释和旧装备配置。已通过 API 将 `equip_slots` 补为 `1..12`，`equip_intensify_min` 补为 `7`，但需要后续确认部署初始化是否应覆盖或迁移旧配置。

排查修复：`2026-06-18 18:05` 后，针对 600 扩容慢、缩容慢、`autoStop` 慢进行调度修复并重新部署验证。

- [x] 根因确认：调度先执行回收/分配，再按维护间隔对齐目标，目标变化不能立即优先生效。
- [x] 根因确认：UID 分配每个 actor 都执行一次 `selectRobots`，不足时逐个 `CreateRobots(Count=1)`，600 扩容时 DB 扫描和创建被放大。
- [x] 根因确认：actor 自动上线使用同步确认，单个 UID 可等待 `online_confirm_timeout_ms=60000`，释放/停止控制消息要等当前上线确认返回。
- [x] 已改为每秒优先对齐目标 actor 数，扩容/缩容不再等待维护间隔。
- [x] 已改为批量扫描可用 UID、批量创建缺口，并按小 UID 顺序分配给 actor。
- [x] 自动 actor 上线改为只发送上线请求，后续由 actor 状态轮询确认 running。
- [x] 上线请求已 accepted 但长期未 running 的 UID 会进入健康失败窗口，超过 `bad_recover_sec` 后回收。
- [x] 已移除不再生效的 `auto_online_maintain_interval_sec` 配置项和相关代码字段。
- [x] 本地 `go test ./...` 通过。
- [x] 本地 `go vet ./...` 通过。
- [x] Linux 版 `robot` 构建通过并部署到 `/root/robot`。
- [x] 默认 20 人重启验证通过：`actors=20 leased=20 idle=0`，Web 列表从 `17000000` 开始。
- [x] `20 -> 100` 验证通过：约 10 秒达到 `actors=100 leased=100`。
- [x] `100 -> 20` 验证通过：约 10 秒恢复到 `actors=20 leased=20`，高 UID 已 logout。
- [x] `autoStop` 验证通过：约 10 秒到 `actors=0 leased=0 connecting=0`，动作计数停止增长。
- [x] `20 -> 600` 短压测验证通过：约 20 秒达到 `actors=600 leased=600 idle=0`。
- [x] 600 人 3 分钟内调度稳定：`leased=600` 持续稳定，`running` 随摆摊状态在约 `574..599` 波动，`connecting` 约 `0..3`。
- [x] 600 人 3 分钟内功能持续增长：移动、普通喊话、全服喊话、摆摊成功/失败/过期均有增长。
- [x] `600 -> 20` 验证通过：约 10 秒恢复到 `actors=20 leased=20 connecting=0`。
- [x] `600 -> 20` 后在线 UID 保持小号：Web 返回 `online_minmax=(17000000,17000019)`。
- [x] 600 人 10 分钟首次完整复测发现误回收问题：日志出现 `state=running reason=online_confirm_timeout`，缩容后 Web 列表缺少 `17000005`、`17000020` 等已被误 cleanup 的 UID。
- [x] 根因确认：actor 已 running 后没有清空 `lastOnlineTry`，旧上线确认计时器在后续状态短暂波动时触发 `online_confirm_timeout`。
- [x] 已修复：`markOnlineHealthy` 清空 `lastOnlineTry/firstFailureAt`，running actor 不再被旧上线计时器误回收。
- [x] 已补单测：健康 actor 会清空上线确认计时器。
- [x] 修复后 600 人 3 分钟回归通过：`actors=600 leased=600 idle=0`，Web 列表 `returned=600`、`missing_count=0`。
- [x] 修复后日志确认无新的 `recycle_uid`/`online_confirm_timeout` 误回收。
- [x] 修复后 600 人 10 分钟完整压测通过：全程 `actors=600 leased=600 idle=0`，结束 `running=581 connecting=1`。
- [x] 修复后 600 人 10 分钟动作增量：`online +762/0`、`move +56767/0`、`local +25354/0`、`world +25369/0`、`store +46/140`、`expired +42`。
- [x] 修复后 600 人 10 分钟 Web 列表正常：`returned=600`，UID `17000000..17000599` 连续，`missing_count=0`。
- [x] 修复后 600 人 10 分钟资源未失控：采样约 `CPU 39%..77% / 167..210MB / 1347..1353 goroutines`。
- [x] 修复后 `600 -> 20` 验证通过：约 10 秒恢复到 `actors=20 leased=20 running=20 connecting=0`。
- [x] 修复后 `600 -> 20` 在线 UID 保持小号：Web 返回 `online_minmax=(17000000,17000019)`，列表仍 `returned=600 missing_count=0`。
- [x] Key 缺失/恢复验证通过：临时移动 `/home/neople/game/privatekey.pem` 后 `keypairStatus.game_valid=false key_state=invalid`。
- [x] Key 缺失时调度阻断：约 15 秒内 `actors=0 leased=0 running=0 connecting=0`，不继续上线。
- [x] Key 恢复后调度恢复：移回 privatekey 后 `keypairStatus.game_valid=true`，自动恢复到 `actors=20 leased=20 running=20`。
- [x] 游戏端口关闭/恢复验证通过：临时将 `/root/config/config.ini` 的 `RobotGamePort` 改为 `19999` 并重启 robot。
- [x] 游戏端口关闭时调度阻断：`game_port_ready=false`，地址 `192.168.200.131:19999`，`actors=0 leased=0 running=0 connecting=0`。
- [x] 游戏端口恢复后调度恢复：还原 `RobotGamePort=10011` 并重启，稳定窗口后恢复到 `actors=20 leased=20 running=20`。
- [x] 创建角色装备探针完成：清理并重建高 UID `17000590..17000599`，确认创建逻辑复用原 UID 段。
- [x] 新建角色武器检查通过：10/10 有武器，武器强化均为 `+8..+15`。
- [x] 新建角色称号检查通过：10/10 有称号；称号按当前写入逻辑强化为 `0`，不纳入 `+7` 普通装备判断。
- [x] 新建角色普通装备强化检查通过：已写入的非武器、非称号装备均为 `+7` 或以上。
- [x] 新建角色魔法石检查通过：5/10 有魔法石；缺魔法石的 5 个角色按当前 PVF 职业/等级/稀有度过滤候选数均为 `0`，符合“有单件就装，单件都没有就算了”。
- [x] 100 人合并故障恢复场景完成：目标 `100`，摆摊概率临时改为 `100%`，`actors=100 leased=100 idle=0` 稳定。
- [x] 手动选中 UID 操作补测通过：样本 UID `17000000`，手动移动、普通喊话、全服喊话均 `accepted=1 confirmed=1 failed=0`。
- [x] 手动异步操作补测通过：样本 UID `17000000`，手动摆摊、下线、上线均返回 `state=queued`。
- [x] `autoStop` 摆摊中断补测通过：目标 `100` 且有摆摊运行时停止，约 10 秒到 `actors=0 leased=0 connecting=0`。
- [x] 摆摊中断日志补测通过：日志出现 `[AutoStore] uid=... cancelled_before_try=2`。
- [x] `autoStart` 恢复补测通过：停止后重新启动目标 `100`，约 20 秒恢复到 `actors=100 leased=100 connecting=0`。
- [x] kill robot 后重启恢复补测通过：`kill -9` robot 后重新拉起，约 25 秒恢复到 `actors=100 leased=100`。
- [x] kill robot 后自动调度重新建立 actor，未观察到重复租约；重启后计数从 0 重新开始，符合进程内统计重置预期。
- [x] 合并场景结束后恢复默认目标 `20`、摆摊概率 `20%`、摆摊间隔 `60..180`、摆摊并发 `30`。
- [x] 合并场景日志扫描完成：本轮日志未出现 `msg_queue_full`、`panic`、`fatal`、`database is closed`、`stop_timeout`。

继续执行：`2026-06-18 21:15` 后，按 `doc/vm连接信息.md` 使用 Python `subprocess.run([...])` 调用 VM 工具，只覆盖 Web 手工操作和游戏内观察以外的调试项。

- [x] 远端 API/SSH 连通性确认通过，调试前状态为 `target_online=25 actors=25 leased=25`，无后台调试进程残留后继续。
- [x] 坏 UID 回收补测通过：将当前在线 UID `17000002` 的 `Dummylist.port` 临时改为 `19999` 并触发下线，日志在 `21:26:01` 出现 `recycle_uid slot=106 uid=17000002 reason=failure_window`，`21:26:07` 出现 `recycle_cleanup_done uid=17000002 deleted=1 skipped=0`。
- [x] 坏 UID 回收期间 actor 数没有膨胀：回收后仍保持 `actors=25 leased=25 idle=0 recycling=0 blocked_uids=0`，调度补租后继续运行。
- [x] 坏 UID cleanup 后数据库层面确认 `17000002` 已释放，后续可由创建逻辑重新从可用 UID 段补回；当前低号段因历史 600 压测和回收存在空洞，但调度仍优先租用现存小 UID。
- [x] 远端旧 `robot_config.ini` 已用本地默认配置覆盖，确认不再包含 `gateway` 注释和旧 `[debug_spawn]` 配置。
- [x] 调试结束恢复默认运行配置：`auto_target_online_count=20`、`auto_store_probability_percent=20`、`scheduler.bad_failures=3`、`scheduler.bad_recover_sec=60`、`scheduler.store_concurrent=30`。
- [x] 恢复默认后约 4 秒收敛到 `target_online=20 actors=20 leased=20 idle=0 recycling=0 blocked_uids=0`；`running` 随摆摊/连接状态波动，仅作状态参考。
- [x] 本轮按用户要求未执行 Web 页面手工操作和游戏内可视观察；Web/API 调用仅用于状态读取、配置写入和自动调度控制。

## 基础信息

- VM IP：`192.168.200.131`
- SSH：`root / 123456`
- Web：`http://192.168.200.131:8112`
- TCP API：`192.168.200.131:8111`
- 游戏端口：`10011`
- robot：`/root/robot`
- 配置目录：`/root/config`
- 主配置：`/root/config/config.ini`
- 行为配置：`/root/config/robot_config.ini`
- 日志：`/root/config/log_robot`
- 启动全服：`/root/run`
- 停止全服：`/root/stop`
- 最后快照：`快照 2`

## 执行原则

- [ ] 能在同一次压测或恢复流程中观察的项目合并观察，不重复单独跑。
- [ ] 记录每个阶段开始/结束时间、目标人数、日志偏移、关键计数快照。
- [ ] `running` 只能作为状态参考，不能作为补 actor 或判断目标达成的唯一依据。
- [ ] 判断调度是否正确时，以 `target/actors/leased/idle/recycling/blocked_uids` 为主。
- [ ] 正常停止、目标下降只 logout，不 cleanup 正常 UID。
- [ ] 坏 UID 回收才允许 cleanup，且 actor 数不应因此增加。

## 调试前准备

- [ ] 记录当前 git commit。
- [ ] `go test ./...` 通过。
- [ ] `go vet ./...` 通过。
- [ ] Linux 版 `robot` 构建通过。
- [ ] VM 可 SSH 登录。
- [ ] `/root/robot` 已备份。
- [ ] 新二进制已部署到 `/root/robot`。
- [ ] `/root/config` 中主配置和行为配置存在。
- [ ] `robot_config.ini` 使用 `[spawn]`，没有旧 `debug_spawn` 配置。
- [ ] `/root/config/log_robot` 可写。
- [ ] Web 可登录。
- [ ] TCP API 可调用。
- [ ] 游戏端口 `10011` 可连接。
- [ ] 游戏服 `max_user_num` 满足本次压测目标。
- [ ] 记录日志偏移，避免旧日志干扰判断。

## 基础 API 与 Web

- [ ] `autoStatus` 正常返回。
- [ ] `systemStatus` 正常返回。
- [ ] `keypairStatus` 正常返回。
- [ ] `robotConfigGet` 正常返回。
- [ ] `robotsStatus count=10` 正常返回。
- [ ] `robotsStatus count=10000` 正常返回，Web 假人列表显示全部假人。
- [ ] Web 顶部显示 `运行/目标`，并与 `autoStatus.running/target_online` 一致。
- [ ] Web 顶部 CPU、内存、goroutine 正常刷新。
- [ ] Web 游戏端口状态正常刷新。
- [ ] Web Key 状态正常刷新。
- [ ] Web 日志按钮可读取 `/root/config/log_robot`。
- [ ] Web 配置弹窗可读取和保存 `robot_config.ini`。

## Key 与游戏端口阻断

### Key 正常

- [ ] `keypairStatus.game_valid=true`。
- [ ] Web 业务按钮可用。
- [ ] 自动调度可启动。

### Key 缺失或无效

- [ ] 临时移动一个 game key 文件。
- [ ] `keypairStatus` 显示缺失或无效。
- [ ] Web Key 状态变为异常。
- [ ] Web 业务按钮禁用或业务命令返回 Key 错误。
- [ ] 自动调度停止新增 actor。
- [ ] 自动调度停止分配 UID。
- [ ] 日志出现 `auto_blocked key_state=...`。
- [ ] 恢复 key 后 `keypairStatus` 正常。
- [ ] 恢复 key 后自动调度可继续。

### 游戏端口关闭与恢复

- [ ] 停止游戏服或临时把 `RobotGamePort` 指向未监听端口。
- [ ] `autoStatus.game_port_ready=false`。
- [ ] Web 游戏端口显示未开放。
- [ ] 自动调度不创建新 actor。
- [ ] 自动调度不分配 UID。
- [ ] 日志无上线失败风暴。
- [ ] 恢复游戏端口。
- [ ] `game_port_ready=true`。
- [ ] `game_port_stable_at` 更新。
- [ ] 自动调度恢复到目标 actor/租约数量。

## 配置热更新

- [ ] 修改目标人数后，`robot_config.ini` 更新。
- [ ] 修改目标人数后，`autoStatus.target_online` 更新。
- [ ] 不重启 robot 即可生效。
- [ ] 修改移动间隔后，`move_success` 增长频率变化。
- [ ] 修改普通/全服喊话间隔后，两类喊话增长频率变化。
- [ ] 修改摆摊概率后，摆摊触发频率变化。
- [ ] 修改摆摊并发后，`store_running` 不超过配置值。

## 默认启动观察

建议使用默认配置目标 `20`，运行 `10 分钟`。

- [ ] 默认自动行为开启。
- [ ] 默认目标人数为 `20`。
- [ ] 默认摆摊概率为 `20%`。
- [ ] 默认装备槽包含武器、称号、魔法石。
- [ ] `actors=target`。
- [ ] `leased<=actors`。
- [ ] `idle` 不长期大量存在。
- [ ] 自动移动、普通喊话、全服喊话、摆摊都有机会触发。
- [ ] Web/API 在运行期间可用。
- [ ] CPU、内存、goroutine 稳定。
- [ ] 无 `msg_queue_full`。
- [ ] 无 actor 或 UID 膨胀趋势。

## 创建角色与装备

- [ ] 新建假人优先从小 UID 开始。
- [ ] UID 和名字在 cleanup 后可复用。
- [ ] 新建角色后可正常上线。
- [ ] 武器槽已装备。
- [ ] 称号槽已装备。
- [ ] 魔法石槽有可用 PVF 物品时已装备。
- [ ] 普通装备强化为 `+7` 或以上。
- [ ] 武器强化为 `+8..+15`。
- [ ] 武器按职业、性别等条件过滤，不做放宽兜底。
- [ ] 装备、称号、魔法石写入不导致登录失败。

## 调度与 UID 顺序

以下项目可在 `20 -> 100 -> 20` 或 `100 -> 600 -> 20` 同一次升降目标中合并观察。

- [ ] 扩容时 actor 数逐步达到目标。
- [ ] 扩容时 `leased` 逐步达到目标。
- [ ] 扩容时优先租用小 UID。
- [ ] 扩容期间不重复租用同一个 UID。
- [ ] 不因为 `running` 短暂波动补 actor。
- [ ] 缩容时优先停止大 UID actor。
- [ ] 缩容后保留小 UID，Web 列表前段可观察。
- [ ] 缩容后 `actors=target`。
- [ ] 缩容后 `leased<=target`。
- [ ] 缩容释放的 UID 只 logout，不 cleanup。
- [ ] 缩容后多余 actor 不再移动、喊话、摆摊。
- [ ] `autoStop` 后所有 auto actor 停止。
- [ ] `autoStop` 后当前租约 UID logout。
- [ ] `autoStop` 后动作计数停止增长。

## 生命周期与 UID 回收

- [ ] actor 分配 UID 后进入上线流程。
- [ ] 上线成功后进入 running。
- [ ] `online_success` 增加。
- [ ] 上线失败后 `online_failed` 增加。
- [ ] 连续失败达到 `scheduler.bad_failures` 后标记回收。
- [ ] 超过 `bad_recover_sec` 后标记回收。
- [ ] 日志出现 `recycle_uid`。
- [ ] 回收前 actor release/logout 当前 UID。
- [ ] Supervisor 调用 cleanup。
- [ ] cleanup 成功后日志出现 `recycle_cleanup_done`。
- [ ] cleanup 失败后日志出现 `recycle_cleanup_failed`。
- [ ] cleanup 失败 UID 加入 blocked。
- [ ] blocked UID 本进程内不再分配。
- [ ] 回收完成后 actor 回到 idle。
- [ ] actor 重新分配新 UID。
- [ ] 回收期间 actor 总数不增加。

## 动作合并观察

以下项目可在默认 20、小规模 100、大规模 600 压测中同时观察，不需要分别重复跑。

- [ ] 自动移动触发。
- [ ] `move_success` 持续增长。
- [ ] `move_failed` 不持续增长。
- [ ] 假人坐标在 Web/API 中变化。
- [ ] 摆摊状态下该 actor 不移动。
- [ ] 自动普通喊话触发。
- [ ] `shout_local_success` 持续增长。
- [ ] 日志出现 `channel=local type=3`。
- [ ] 游戏内可观察到普通喊话。
- [ ] 自动全服喊话触发。
- [ ] `shout_world_success` 持续增长。
- [ ] 日志出现 `channel=world type=80`。
- [ ] 游戏内可观察到全服喊话。
- [ ] 普通喊话和全服喊话互相独立。
- [ ] 普通喊话不会落成全服喊话。
- [ ] 全服喊话不会落成普通喊话。
- [ ] 动作不会堆积任务。

## 摆摊生命周期

摆摊成功、失败、过期、终止可在 20 人默认运行、提高摆摊概率、缩容/停止时合并观察。

- [ ] 自动摆摊触发。
- [ ] actor 触发摆摊后不被调度当成空位。
- [ ] `store_running` 不超过 `scheduler.store_concurrent`。
- [ ] 日志出现 `[AutoStore] uid=... try=...`。
- [ ] 日志出现 `[StoreSuccessPoint]` 或 `[StoreDisplay]`。
- [ ] Web 列表显示摆摊状态。
- [ ] 游戏内可观察到摊位。
- [ ] 摆摊成功后 `store_success` 增加。
- [ ] 摆摊失败会换坐标重试。
- [ ] 达到最大重试后 `store_failed` 增加。
- [ ] 完整摆摊失败记入 UID 失败。
- [ ] 摆摊成功后等待 `auto_store_duration_sec`。
- [ ] 到期后 `store_expired` 增加。
- [ ] 到期后 actor logout 摆摊 UID。
- [ ] 权限和私店状态被回收。
- [ ] 假人恢复普通位置。
- [ ] actor 继续后续生命周期。
- [ ] `autoStop` 或缩容时，摆摊流程尽快中断。
- [ ] 中断日志出现 `cancelled_before_try` 或等价退出原因。
- [ ] 正常停止/缩容不 cleanup 正常 UID。

## 手动操作

- [ ] 未选择假人点“上线”，弹出上线数量。
- [ ] 已选择假人点“上线”，直接对选中 UID 执行上线。
- [ ] 手动上线指定数量。
- [ ] 手动移动指定 UID。
- [ ] 手动普通喊话指定 UID。
- [ ] 手动全服喊话指定 UID。
- [ ] 手动摆摊指定 UID。
- [ ] 手动下线指定 UID。
- [ ] 已被 auto actor 租用的 UID，命令投递到对应 actor。
- [ ] 未被 actor 租用的 UID，直接走基础能力，不创建额外 actor。
- [ ] 手动操作不破坏 auto actor 租约。

## 压测合并观察

### 100 人 10 分钟

- [ ] 目标设置为 `100`。
- [ ] `actors=100` 稳定。
- [ ] `leased<=100`。
- [ ] `idle` 不长期大量存在。
- [ ] `blocked_uids` 不持续快速增长。
- [ ] 移动、普通喊话、全服喊话、摆摊全部持续发生。
- [ ] Web/API 可用。
- [ ] 无 `msg_queue_full`。
- [ ] 无 panic/fatal。
- [ ] CPU、内存、goroutine 稳定。
- [ ] 无 actor 或 UID 无限增长趋势。

### 600 人 10 分钟

600 压测同时覆盖 Web/API 可用性、动作持续增长、摆摊并发、running 波动、资源占用、UID 膨胀、恢复到 20 后的缩容行为，不再为这些项目单独重复跑。

- [ ] 目标设置为 `600`。
- [ ] `actors=600` 稳定。
- [ ] `leased<=600`。
- [ ] `running` 可低于 600，但与摆摊、连接、租约状态一致。
- [ ] `idle` 不长期大量存在。
- [ ] `blocked_uids` 不持续快速增长。
- [ ] 移动、普通喊话、全服喊话、摆摊全部持续发生。
- [ ] `store_running` 不超过配置并发。
- [ ] Web 列表可刷新。
- [ ] `robotsStatus count=10000` 响应时间可接受。
- [ ] `autoStatus/systemStatus` 响应时间可接受。
- [ ] `msg_queue_full` 为 0。
- [ ] 无 panic/fatal。
- [ ] robot CPU 不长期 100%。
- [ ] 内存不持续线性增长。
- [ ] goroutine 不持续线性增长。
- [ ] UID 回收发生时 actor 数不增加。
- [ ] cleanup 后 UID/名字不无限膨胀。
- [ ] 从 `600` 恢复到 `20` 后，优先释放大 UID。
- [ ] 从 `600` 恢复到 `20` 后，保留小 UID。
- [ ] 从 `600` 恢复到 `20` 后，多余动作停止增长。

## 故障注入

故障注入优先在 100 或 600 压测窗口中执行，避免重复启动一轮。

- [ ] 压测中 kill robot。
- [ ] robot 重启后 actor 重新建立。
- [ ] robot 重启后能接管已有 active UID。
- [ ] 接管 active UID 时不重复上线。
- [ ] 压测中短暂关闭游戏端口。
- [ ] 游戏端口关闭期间不创建 actor、不分配 UID。
- [ ] 游戏端口恢复后调度继续。
- [ ] 压测中破坏一个 UID。
- [ ] 坏 UID 触发 release、cleanup、重新分配。
- [ ] 压测中删除 key。
- [ ] key 异常期间调度停止新增，业务命令阻断。
- [ ] 恢复 key 后调度继续。

## 日志关键字

- [ ] `[RobotMetrics]`
- [ ] `[RobotSupervisor] recycle_uid`
- [ ] `[RobotSupervisor] recycle_cleanup_done`
- [ ] `[RobotSupervisor] recycle_cleanup_failed`
- [ ] `[RobotActor] online_failed`
- [ ] `[AutoStore]`
- [ ] `[StoreDisplay]`
- [ ] `[StoreSuccessPoint]`
- [ ] `[Shout]`
- [ ] `msg_queue_full`
- [ ] `panic`
- [ ] `fatal`
- [ ] `ERROR`
- [ ] `database is closed`
- [ ] `stop_timeout`

## 通过标准

- [ ] 基础 API 和 Web 可用。
- [ ] Key 异常能阻断，恢复后能继续。
- [ ] 游戏端口异常时不制造上线风暴。
- [ ] actor 数严格跟随目标。
- [ ] 不因 running 波动补 actor。
- [ ] UID 按小号优先租用、按大号优先释放。
- [ ] UID 坏掉后被释放、cleanup、重新分配。
- [ ] 正常停止和目标下降只 logout，不 cleanup 正常 UID。
- [ ] 普通喊话和全服喊话都真实发生且互相独立。
- [ ] 摆摊成功、失败、过期、终止路径都可观察。
- [ ] 无 `msg_queue_full`。
- [ ] CPU、内存、goroutine 无持续失控。

## 回滚与恢复

- [ ] 每次部署前记录当前 git commit。
- [ ] 每次部署前备份 `/root/robot`。
- [ ] 出现不可恢复异常时停止 robot。
- [ ] 必要时恢复 `/root/robot` 备份。
- [ ] 必要时恢复 VM `快照 2`。
- [ ] 恢复后重新执行基础 API 检查。
